#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int i,count=0;
    int n;
    scanf("%d",&n);
    for(i=2;i<sqrt(n);i++)
    {
        if(n%i==0)

        {
            count++;

        }

    }
    if(count==0)
        printf("prime");
        else
        printf("not prime");
    return 0;
}
